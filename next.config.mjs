/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/threejs-geojson-map',
    images: {
      unoptimized: true,
    },
  };
  
  export default nextConfig;